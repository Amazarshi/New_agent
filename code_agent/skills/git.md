# Git 技能

当任务涉及 Git 时，遵守这些规则：

1. 先查看 git status，确认当前工作区状态。
2. 不要随便回滚用户已有修改。
3. 不要执行 git reset --hard，除非用户明确要求。
4. 提交前先说明会提交哪些文件。
5. 查看差异时优先使用 git diff。

常用命令：

- git status
- git diff
- git log --oneline -5